# Copy this file to `.env` and fill in your secrets.
# `.env` is gitignored and will never be committed.
# `.env.example` IS tracked by git — keep it sanitized (placeholders only).

# CoinGecko free demo API key (sign up at https://www.coingecko.com/en/api/pricing).
# Free demo tier: ~30 calls/min, 10k calls/month, 365-day history limit.
# Reserved for future Pro-tier robustness checks per specs/08_universe.md.
COINGECKO_API_KEY=your_demo_key_here
